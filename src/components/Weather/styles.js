import styled from 'styled-components'

export const WeatherBox = styled.div`
  margin: 8px 0;

  .wrapper {
    display: flex;
    margin-top: 36px;
    align-items: center;
  }

  .mimTemp {
    color: blue;
  }

  .maxTemp {
    color: red;
  }

  .img {
    margin-right: 8px;
  }
`
